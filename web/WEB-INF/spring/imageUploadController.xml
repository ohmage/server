<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:util="http://www.springframework.org/schema/util"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-2.5.xsd
                           http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util-2.5.xsd">
  <!--  
     Spring ApplicationContext configuration for media data upload (jpeg images for now, but video, audio, text, in the future)  
  -->
  
  <!-- Servlet-to-AW hooks see web.xml -->
  <!-- pre-validate -->
  <bean id="imageUploadHttpServletRequestValidator" class="org.ohmage.jee.servlet.validator.MediaUploadValidator">
    <constructor-arg index="0"><ref bean="diskItemFileFactory" /></constructor-arg>
    <!-- The maximum size in bytes of a request's file attachment -->
    <constructor-arg index="1"><value>2097152</value></constructor-arg>
  </bean>
  <!-- create AwRequest from data in the HttpServletRequest -->
  <bean id="imageUploadAwRequestCreator" class="org.ohmage.jee.servlet.glue.MediaUploadAwRequestCreator" />
  <!-- generate output - see successOrFailResponseWriter in mobilityUploadController.xml -->
  
  <!-- Controller - main workflow -->
  <bean id="imageUploadController" class="org.ohmage.controller.ControllerImpl">
    <constructor-arg index="0">
    <!-- service the upload request -->
      <list>
        <!-- authenticate -->
        <ref bean="authWithHashedPasswordService" />
        
        <!-- authorize -->
        <ref bean="userRoleCampaignPopulationService" />
        <ref bean="userCampaignValidationService" />
        <ref bean="participantUserRoleValidationService" />
        <ref bean="campaignRunningStateValidationService" />
        <ref bean="campaignCreationTimeValidationService" />
        
        <!-- save the image -->
        <ref bean="imageUploadDaoService" />
      </list>
    </constructor-arg>
    <constructor-arg index="1">
      <ref bean="serverErrorRequestAnnotator" />
    </constructor-arg>
    <property name="validators">
      <list>
        <ref bean="userNameValidator" />
        <ref bean="hashedPasswordValidator" />
        <!-- TODO need to check that there is a valid jpeg in awRequest.getMedia() --> 
        
        <!-- any validator that simply checked for a empty string has been removed because it was redundant with 
             validation performed in the servlet world -->
      </list>
    </property>
    <!-- <property name="postProcessingService"> -->
      <!-- log upload stats, the upload itself, any failed upload info, and duplicates to the filesystem -->  
     <!-- <bean id="messageLoggerService" class="org.ohmage.service.MessageLoggerService">
        <constructor-arg index="0">
          <value>media</value>
        </constructor-arg>
      </bean>
    </property> -->
    <property name="featureName"><value>image upload</value></property>
  </bean>
  
  <bean id="imageUploadDaoService" class="org.ohmage.service.DaoService">
    <constructor-arg index="0"><ref bean="imageUploadDao" /></constructor-arg>
  </bean> 
  
  <bean id ="imageUploadDao" class="org.ohmage.dao.UrlBasedResourceDao">
    <constructor-arg index="0"><ref bean="dataSource" /></constructor-arg> <!-- database access -->
    <constructor-arg index="1" value="${system.imageDir}"/> <!-- root directory for storage -->
    <constructor-arg index="2" value=".jpg" /> <!-- file extension -->
    <constructor-arg index="3" value="1000" /> <!-- max number of dirs -->
    <constructor-arg index="4" value="1000" /> <!-- max number of files -->
  </bean>
  
</beans>