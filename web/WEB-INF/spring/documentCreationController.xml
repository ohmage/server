<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:util="http://www.springframework.org/schema/util"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-2.5.xsd
                           http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util-2.5.xsd">

  <!-- Basic HTTP Validation -->
  <bean id="documentCreationHttpServletRequestValidator" class="org.ohmage.jee.servlet.validator.DocumentCreationValidator">
    <!-- Read the XML file. -->
    <constructor-arg index="0"><ref bean="diskItemFileFactory" /></constructor-arg>
  </bean>
  
  <!-- Create the AwRequest Object -->
  <bean id="documentCreationAwRequestCreator" class="org.ohmage.jee.servlet.glue.DocumentCreationAwRequestCreator" />
  
  <!-- Write the Response -->
  <bean id="documentCreationResponseWriter" class="org.ohmage.jee.servlet.writer.DocumentCreationResponseWriter">
    <constructor-arg index="0">
      <bean class="org.ohmage.domain.ErrorResponse">
        <property name="code" value="1500" />
        <property name="text" value="General document creation error." />
      </bean>
    </constructor-arg>
  </bean>
  
  <!-- Controller Definition -->
  <bean id="documentCreationController" class="org.ohmage.controller.ControllerImpl">
    <constructor-arg index="0">
      <!-- Services List (expected to be executed in-order) -->
      <list>
        <!-- Authenticate that the token the user is using is a valid token. -->
        <ref bean="tokenBasedAuthenticationService" />
        
        <!-- Ensure that the classes and campaigns do exist. -->
        <ref bean="campaignUrnsInDocumentRoleListExistOptionalService" />
        <ref bean="classUrnsInDocumentRoleListExistOptionalService" />
        
        <!-- Ensure that the user has the appropriate roles in the campaign. -->
        <ref bean="userRoleCampaignPopulationService" />
        <ref bean="userCanAssociateDocumentsWithCampaignsInCampaignRoleListOptionalService" />
        
        <!--  Ensure that the user has the appropriate roles in the class. -->
        <ref bean="userRoleClassPopulationService" />
        <ref bean="userCanAssociateDocumentsWithClassesInClassRoleListOptionalService" />
        
        <!-- Insert the document. -->
        <ref bean="documentCreationService" />
      </list>
    </constructor-arg>
    <constructor-arg index="1">
      <!-- The annotator for when something fails after validation but before the writer. -->
      <ref bean="serverErrorRequestAnnotator" />
    </constructor-arg>
    <property name="validators">
      <!-- Validates the individual items in the request. -->
      <list>
        <!-- Validate each of the items in the request. -->
        <ref bean="userTokenValidator" />
        <ref bean="documentNameRequiredValidator" />
        <ref bean="documentRequiredValidator" />
        <ref bean="documentPrivacyStateRequiredValidator" />
        <ref bean="documentCampaignRoleOptionalValidator" />
        <ref bean="documentClassRoleOptionalValidator" />
        <ref bean="descriptionOptionalValidator" />
      </list>
    </property>
    <property name="featureName"><value>document creation</value></property>
  </bean>
  
  <bean id="documentCreationService" class="org.ohmage.service.DocumentCreationService">
    <constructor-arg>
      <ref bean="documentManipulationError" />
    </constructor-arg>
    <constructor-arg>
      <bean class="org.ohmage.dao.DocumentCreationDao">
        <constructor-arg>
          <ref bean="dataSource"/>
        </constructor-arg>
        <constructor-arg>
          <util:constant static-field="org.ohmage.cache.PreferenceCache.KEY_DOCUMENT_DIRECTORY"/>
        </constructor-arg>
      </bean>
    </constructor-arg>
  </bean>
  
</beans>