The files in bin and conf are specific to Linux and to a specific version of MySQL.

For other OSes and versions of MySQL (5.xx), simple tweaks to the OS- and version-specific values should suffice for running the
AndWellness db server with the same configuration as the deployed version. 
